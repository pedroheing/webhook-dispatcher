{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Event API",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/event",
            "cwd": "${workspaceFolder}"
        }
    ]
}